{
  "name": "bak-js",
  "module": "index.ts",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "clean": "rm -rf build && mkdir build",
    // takes 1m55.524s to run
    "build": "./build.sh",
    "start": "cd build && ./main3d",
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "~5.6.3"
  }
}